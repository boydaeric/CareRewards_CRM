streamlit>=1.31.0
pandas>=2.2.0
plotly>=5.18.0
```

**Why this works:**
- Removes exact version pins that force old pandas
- `pandas>=2.2.0` is fully Python 3.13 compatible
- Streamlit Cloud will auto-select compatible versions

---

## **Quick Fix Steps**

### **Option 1: GitHub Web Interface (Fastest - 2 Minutes)**

1. Go to your GitHub repo: `github.com/YOUR_USERNAME/carerewards_crm`
2. Click on `requirements.txt`
3. Click the **pencil icon** (top right) to edit
4. Replace entire contents with:
```
   streamlit>=1.31.0
   pandas>=2.2.0
   plotly>=5.18.0
